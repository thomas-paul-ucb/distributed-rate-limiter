# distributed-rate-limiter
A production-grade rate limiter as a standalone service sits in front of APIs using Redis + Lua scripts for atomic operations, supporting multiple algorithms deployable as a sidecar.
